function BucketList(props) {
    return (
        <>
        <div>This is the BucketList</div>
        {/* <section>
            {state.lists.map((l) => (
                <article key={l.list}>
                <div>{l.list}</div>
                </article>
            ))}
            <form on Submit={addList}>
                <label>
                    <span>BucketList</span>
                    <input 
                    name="list"
                    value={state.newList.list}
                    onChange={handleChange}
                    />
                </label>
                <button>Add BucketList</button>
            </form>
        </section> */}
        </>

    );
};

export default BucketList;